BUG FIX:
01.) problema nel'apertura dei file, per esempio
    1. C1 apre file1
    2. C2 apre file1
    3. C1 scrive "test"
    4. C2 apre file2 <-- bug (file2 non è vuoto ma esce scritto "test")
                             (comunque se C2 cancella "test" il file1 aperto in C1 rimane invariato, come è giusto che sia)
    5. C2 cancella "test"
    6. C2 riapre file1 <-- bug (crash);


BUG:
02.) schermata "Loading" rimane aperta (non sempre) quando si aprono/chiudono file (non ho ben capito quando si verifica di preciso).
03.) nel textEdit bisogna settare uno stile e un dimensione del carattere, perchè all'inizio quando si scrive qualcosa e poi si cancella lo stile e la dimensione cambiano.
04.) impostare anche l'esensione ai caratteri speciali anche per il login e per la registrazione.

- tasto di share all'interno dell editor fa crashare il client